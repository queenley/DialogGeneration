# GENERATE DIALOG WITH CHATGPT

## Install
```
pip install -r requirements.txt
```
## Generate one dialog
```
python main.py --key <openai_key> 
```

```
usage: Dialog Generation [-h] --key KEY [--domain DOMAIN] [--domain_type DOMAIN_TYPE] [--product PRODUCT] [--example EXAMPLE] [--user_action USER_ACTION] [--system_action SYSTEM_ACTION] [--slot SLOT] [--intent INTENT] [--num_dialog NUM_DIALOG] [--list_product LIST_PRODUCT]
                         [--save_path SAVE_PATH]

optional arguments:
  -h, --help            show this help message and exit
  --key KEY
  --domain DOMAIN
  --domain_type DOMAIN_TYPE
  --product PRODUCT
  --example EXAMPLE
  --user_action USER_ACTION
  --system_action SYSTEM_ACTION
  --slot SLOT
  --intent INTENT
  --num_dialog NUM_DIALOG
  --list_product LIST_PRODUCT
  --save_path SAVE_PATH
```